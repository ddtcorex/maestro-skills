# Plan: Update skills for Govard v1.64.0 audit checks

**Spec:** `docs/superpowers/specs/2026-08-23-govard-v1.64-audit-checks-design.md`
**Branch:** `feat/govard-v1.64-audit-checks` (from `master`)
**Validation gate for every task:** `npx vitest run` (catalog spec) + targeted grep

---

## Task 1 — magento2-linter: profiler check + media guard

**RED:** `grep -c "only audit check" skills/magento2-linter/SKILL.md` returns 1
(before edit); vitest green (no assertion covers this yet).

**GREEN (edits):**
1. Lines 29–37 paragraph: replace

   > `lint` is the only audit check Govard implements today; any other `--checks` value is rejected.

   with

   > Govard v1.64.0 implements two audit checks: `lint` (PHPCS + PHPStan + the pub/media PHP
   > guard) and `profiler` (captures the Magento stock profiler CSV through the project's own
   > web server — see "The Govard-Native Profiler Check" below); any other `--checks` value is
   > rejected.

2. Step 2 of the workflow: after the existing sentence add:

   > Add `,profiler` plus a `--url` to also capture a page-profile artifact
   > (`govard audit run --checks lint,profiler --url https://<host>/`); a bare
   > `govard audit rerun --session <id>` repeats the latest run's check selection.

3. New subsection right after the "No diff or file-list scoping" paragraph (~170–220 words
   excluding code blocks — measure with the sed|wc pipeline):

   ### The lint run's pub/media PHP guard (M2-LINT-MEDIA)

   Content requirements (state mechanism directly, no investigation narrative):
   - The lint toolchain flags every PHP file found under `pub/media` as
     `M2-LINT-MEDIA / PHP file in pub/media`.
   - Treat these as a security incident, not style noise: PHP under pub/media is a classic
     webshell drop zone (GIF89a polyglots included) and may be directly executable.
   - First response: list the files from the report, quarantine them, check access logs for
     requests to those paths, investigate the upload vector, then rerun the audit.
   - The guard runs inside the standard `govard audit run --checks lint` — no extra flag.

**Verify:** greps for new strings present; `"only audit check"` count 0; `npx vitest run` green.

**Commit:** `docs: document profiler check and pub/media guard in magento2-linter`

---

## Task 2 — security-scan + performance-audit: refresh boundary paragraphs

**Edits (same pattern both files):**
- security-scan lines 22–28: keep the no-`security`-check claim; change "As of Govard v1.63.0"
  to "As of Govard v1.64.0" and append: the lint run's toolchain now includes the pub/media PHP
  guard, so a lint pass also covers that specific file-drop attack surface — it is still not a
  vulnerability scan.
- performance-audit lines 25–31: keep the no-`performance`-check claim; same version refresh;
  append: Govard v1.64.0 adds a native `profiler` check that machine-captures the stock
  profiler CSV for one URL — a quick complement to this skill's manual per-page audit, not a
  replacement (no query log, no cross-page matrix, no thresholds analysis).

**Verify:** grep "v1.63.0" in both files returns 0; vitest green.

**Commit:** `docs: refresh audit boundary notes in security-scan and performance-audit`

---

## Task 3 — govard-magento: profiler example in Code Quality Audit

**Edit:** extend the section after the rerun snippet with:

> To also capture a page-profile artifact in the same run:
>
> ```bash
govard audit run --checks lint,profiler --url 'https://<domain>/'
> ```
>
> The profiler CSV lands under the run's `artifacts/profiler/` with a SHA-256 in the result —
> open it in a spreadsheet to read per-timer costs. Details live in `magento2-linter`.

**Verify:** grep `checks lint,profiler` present; vitest green (dispatch-pattern spec still passes).

**Commit:** `docs: add profiler capture example to govard-magento`

---

## Task 4 — AGENTS.md lesson rewrite

**Edit:** replace the lesson bullet body with:

> **Do not claim Govard-native coverage the audit subsystem lacks.**
> `govard audit` implements the `lint` check (PHPCS + PHPStan + the pub/media PHP guard) and,
> since v1.64.0, the `profiler` check; any other `--checks` value is rejected. QA-trio skills
> must state that boundary instead of implying native security/performance gates exist.

**Verify:** grep "implements only" AGENTS.md → 0; vitest green.

**Commit:** `docs: update audit boundary lesson for v1.64.0 checks`

---

## Final gate

- `npx vitest run` — full green
- Repo-wide grep: no `only audit check` / `implements only the .lint. check` claims remain
- `claude plugin validate . --strict` if available locally; otherwise note it for CI
- Push branch, open PR linking the spec
